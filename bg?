#!/bin/bash
#
# Print out the current desktop background by dumping the contents of the cache
# file generated by the set_bg script.

BG_CACHE="$XDG_CACHE_HOME"/curr_wallpaper.cache

[ -f "$BG_CACHE" ] || {
    echo "Wallpaper cache file not found!"
    exit 1
}

cat "$BG_CACHE"
